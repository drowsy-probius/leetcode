#include <iostream>
#include <string>
#include <vector>
#include <queue>
#include <map>

using namespace std;

class Solution
{

};

int main(int argc, char* argv[])
{
  setbuf(stdout, NULL);
  freopen("input.txt", "r", stdin);
  
  int TC;
  Solution solution;
  scanf("%d", &TC);
  while(TC--)
  {
    
  }

  return 0;
}